kind: Service
apiVersion: v1
metadata:
  name: samplemqquarkusapp
  labels:
    app: samplemqquarkusapp
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
  selector:
    app: samplemqquarkusapp